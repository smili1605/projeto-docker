name: CI/CD COMPLETO - Terraform + Docker + DigitalOcean
on:
  push:
    branches: ["main"]

env:
  REGISTRY: docker.io
  IMAGE_NAME: python-app

jobs:
  build-docker:
    name: 🐳 Build Docker Image
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.tag.outputs.sha }}
      image_name: ${{ steps.build.outputs.image }}

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🏷️ Gerar Tag da Imagem
        id: tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT
          echo "📦 Tag gerada: $SHA"

      - name: 🔐 Login no DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🏗️ Construir e Enviar Imagem
        id: build
        run: |
          TAG=${{ steps.tag.outputs.sha }}
          IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/$IMAGE_NAME:$TAG"
          
          echo "🔨 Construindo: $IMAGE"
          docker build -t $IMAGE .
          docker push $IMAGE
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
          echo "✅ Imagem enviada"

  terraform-infra:
    name: ☁️ Provisionar Infraestrutura
    runs-on: ubuntu-latest
    needs: build-docker
    outputs:
      droplet_ip: ${{ steps.get_ip.outputs.droplet_ip }}

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🔍 Verificar Secrets
        run: |
          if [ -z "${{ secrets.DO_TOKEN }}" ]; then
            echo "🚨 ERRO: DO_TOKEN não configurada!"
            exit 1
          fi

      - name: ⚙️ Configurar Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.6.0"

      - name: 🔄 Inicializar Terraform
        working-directory: ./terraform
        run: terraform init -input=false -no-color
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN || secrets.TF_API_TOKEN }}
          TF_INPUT: "false"

      - name: 📋 Criar Plano Terraform
        working-directory: ./terraform
        run: |
          rm -f .terraform.tfstate.lock.info 2>/dev/null || true
          terraform plan \
            -var="do_token=${{ secrets.DO_TOKEN }}" \
            -var="ssh_public_key=${{ secrets.SSH_PUBLIC_KEY }}" \
            -var="ssh_key_name=projeto-docker-$(date +%s)" \
            -input=false -no-color -out=tfplan
          echo "✅ Plano criado"
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN || secrets.TF_API_TOKEN }}

      - name: 🚀 Aplicar Configuração
        id: apply
        working-directory: ./terraform
        run: |
          terraform apply -auto-approve \
            -var="do_token=${{ secrets.DO_TOKEN }}" \
            -var="ssh_public_key=${{ secrets.SSH_PUBLIC_KEY }}" \
            -input=false -no-color 2>&1 || echo "⚠️  Modo VCS detectado"
          
          if terraform output -raw droplet_ip >/dev/null 2>&1; then
            echo "apply_status=success" >> $GITHUB_OUTPUT
          else
            echo "apply_status=vcs_mode" >> $GITHUB_OUTPUT
          fi
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN || secrets.TF_API_TOKEN }}
        continue-on-error: true

      - name: 🌐 Obter IP do Droplet
        id: get_ip
        working-directory: ./terraform
        run: |
          sleep 15
          for i in {1..5}; do
            IP=$(terraform output -raw droplet_ip 2>/dev/null || echo "")
            if [ -n "$IP" ] && [ "$IP" != "null" ] && [ "$IP" != "" ]; then
              echo "✅ IP encontrado: $IP"
              echo "droplet_ip=$IP" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "⏳ Tentativa $i/5 - Aguardando criação do droplet..."
            sleep 10
          done
          echo "⚠️  IP não disponível ainda"
          echo "droplet_ip=PENDING_APPROVAL" >> "$GITHUB_OUTPUT"
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN || secrets.TF_API_TOKEN }}

  deploy:
    name: 🚀 Deploy no Droplet
    runs-on: ubuntu-latest
    needs: [build-docker, terraform-infra]
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🔍 Preparar e Validar Chave SSH
        id: prepare_ssh
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > /tmp/ssh_key_original
          tr -d '\r' < /tmp/ssh_key_original > /tmp/ssh_key_clean
          echo "" >> /tmp/ssh_key_clean
          cp /tmp/ssh_key_clean /tmp/ssh_key
          chmod 600 /tmp/ssh_key
          
          if ssh-keygen -l -f /tmp/ssh_key >/dev/null 2>&1; then
            echo "✅ Chave SSH válida"
          else
            echo "❌ Chave inválida. Atualize o segredo SSH_PRIVATE_KEY"
            exit 1
          fi

      - name: 🔑 Configurar SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🌐 Verificar Conectividade
        id: check_connectivity
        run: |
          IP="${{ needs.terraform-infra.outputs.droplet_ip }}"
          
          if [ "$IP" = "PENDING_APPROVAL" ] || [ -z "$IP" ]; then
            echo "⏳ Aguardando criação do droplet"
            echo "skip_deploy=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          mkdir -p ~/.ssh
          ssh-keyscan -H "$IP" >> ~/.ssh/known_hosts 2>/dev/null
          
          if timeout 30 ssh -o ConnectTimeout=10 -o BatchMode=yes -o StrictHostKeyChecking=no ubuntu@$IP "echo 'OK'" 2>/dev/null; then
            echo "✅ Droplet acessível"
            echo "skip_deploy=false" >> $GITHUB_OUTPUT
          else
            echo "⚠️  Droplet não está respondendo"
            echo "skip_deploy=true" >> $GITHUB_OUTPUT
          fi

      - name: 📤 Enviar Arquivos para Droplet
        if: steps.check_connectivity.outputs.skip_deploy == 'false'
        run: |
          IP="${{ needs.terraform-infra.outputs.droplet_ip }}"
          scp -o StrictHostKeyChecking=no -o ConnectTimeout=30 \
            docker-compose.yml \
            Dockerfile \
            requirements.txt \
            ubuntu@$IP:/home/ubuntu/
          echo "✅ Arquivos enviados"

      - name: 🚀 Executar Deploy
        if: steps.check_connectivity.outputs.skip_deploy == 'false'
        run: |
          IP="${{ needs.terraform-infra.outputs.droplet_ip }}"
          TAG="${{ needs.build-docker.outputs.docker_tag }}"
          
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 ubuntu@$IP "
            cd /home/ubuntu
            cat > .env << 'EOF'
            DOCKER_IMAGE_TAG=$TAG
            DB_USER=${{ secrets.DB_USER || 'postgres' }}
            DB_PASS=${{ secrets.DB_PASS || 'password' }}
            DB_NAME=${{ secrets.DB_NAME || 'appdb' }}
            EOF
            
            if [ -n '${{ secrets.DOCKERHUB_USERNAME }}' ] && [ -n '${{ secrets.DOCKERHUB_TOKEN }}' ]; then
              echo '${{ secrets.DOCKERHUB_TOKEN }}' | docker login -u '${{ secrets.DOCKERHUB_USERNAME }}' --password-stdin
            fi
            
            docker-compose down 2>/dev/null || true
            docker-compose up -d --build
            echo '✅ Deploy completo em http://$IP:8080'
          "
          echo "🎉 Deploy finalizado: http://$IP:8080"

      - name: 📊 Resumo do Deploy
        run: |
          IP="${{ needs.terraform-infra.outputs.droplet_ip }}"
          echo "========================================"
          echo "📋 RESUMO DO DEPLOYMENT"
          echo "========================================"
          echo "🐳 Imagem: ${{ needs.build-docker.outputs.image_name }}"
          echo "🌐 Droplet IP: $IP"
          
          if [ "${{ steps.check_connectivity.outputs.skip_deploy }}" = "true" ]; then
            echo "⏳ AGUARDANDO CRIAÇÃO DO DROPLET"
            echo "1. Acesse: https://app.terraform.io/app/ProjetoDocker/projeto-docker"
            echo "2. Aprove o plano"
            echo "3. Execute este workflow novamente"
          else
            echo "✅ DEPLOY EXECUTADO"
            echo "🌐 Aplicação: http://$IP:8080"
            echo "🔧 Verifique: ssh ubuntu@$IP 'docker logs python-app'"
          fi
          echo "========================================"